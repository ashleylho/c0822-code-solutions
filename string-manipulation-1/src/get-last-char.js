/* exported getLastChar */
// create space for output
// look at the last character of string
// make that value the output
// show the output

function getLastChar(string) {
  var getLastCharResult = string[string.length - 1];
  return getLastCharResult;
}
