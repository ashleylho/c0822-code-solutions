/* exported getFirstChar */
// create space for output
// look at the first letter of the string
// that letter becomes the output
// display the output

function getFirstChar(string) {
  var firstCharResult = string[0];
  return firstCharResult;
}
