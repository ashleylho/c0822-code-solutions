/* exported head */
// create storage for output
// look at the first element of array
// the first element becomes the output
// show the output

function head(array) {
  var headResult = array[0];
  return headResult;
}
