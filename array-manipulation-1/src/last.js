/* exported last */
// create storage for output
// look at last element of the array
// the value of the last element becomes the value of the output
// show output

function last(array) {
  var lastResult = array[array.length - 1];
  return lastResult;
}
