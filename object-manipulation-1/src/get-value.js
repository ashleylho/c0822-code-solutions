/* exported getValue */
// create storage for output
// look inside of the object
// look for the key
// find the value
// assign the value as the output

function getValue(object, key) {
  var getValueResult = object[key];
  return getValueResult;
}
