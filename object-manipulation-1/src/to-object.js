/* exported toObject */
// create storage for output (object)
// look into array
// grab both the key and value pair
// push them both into the ouput
// show output

function toObject(keyValuePair) {
  var toObjectResult = {};
  return toObjectResult;
}
